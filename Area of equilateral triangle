#include<stdio.h>
#include<math.h>

int main(){
    
    int a ;
    float area=0;
    
    printf("enter a side : ");
    scanf("%d", &a);
    
    area= sqrt(3)/4*(a*a);
    printf("Area of equilateral triangle is %f", area);
    
    return 0;
}
